import React from 'react';

const Buttons = () => {
    return (
        <div>
            <button className="btn btn-primary">Click me !</button>
            <button className="btn btn-secoundary">Another button</button>
        </div>
    );
};

export default Buttons;